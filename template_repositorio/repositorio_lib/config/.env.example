# =============================================================================
# ARCHIVO DE CONFIGURACIÓN CENTRALIZADO
# =============================================================================
# Este es el ÚNICO archivo .env válido para todo el proyecto.
# Todos los templates (API, Web, Consola) leen configuración desde aquí.
#
# Ubicación: repositorio_lib/config/.env
# Cargado por: repositorio_lib/config/settings.py
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
DB_USER=your_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_db
DB_ECHO=false

# Connection Pool Settings
POOL_SIZE=10                    # Number of persistent connections
MAX_OVERFLOW=20                 # Maximum overflow connections
POOL_PRE_PING=true              # Test connection before use
POOL_RECYCLE=3600               # Recycle connections after N seconds (1 hour)
POOL_TIMEOUT=30                 # Timeout for getting connection from pool

# =============================================================================
# JWT AUTHENTICATION (for API & Web templates)
# =============================================================================
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=120

# =============================================================================
# ENVIRONMENT & LOGGING
# =============================================================================
ENVIRONMENT=dev                 # dev | production

# Log directories (auto-selected based on ENVIRONMENT)
LOG_DIR_DEV=logs                # Development: relative path
LOG_DIR_PRD=/var/log/app/logs   # Production: absolute path

# =============================================================================
# EMAIL / SMTP (for notifications, reports, etc.)
# =============================================================================
SMTP_HOST=smtp.gmail.com
SMTP_USER=notifications@example.com
SMTP_PASSWORD=your_smtp_password
EMAIL_OPERACIONES=operaciones@example.com
SMTP_USE_TLS=true
SMTP_PORT_TLS=587
SMTP_USE_SSL=false
SMTP_PORT_SSL=465

# =============================================================================
# WORKER/CONSOLE CONFIGURATION (for template_consola)
# =============================================================================
MINUTOS_CONSOLA=60              # Interval between batch cycles (minutes)
ENABLE_CONTINUOUS_MODE=false    # true = loop infinito | false = run once
MAX_RETRIES=3                   # Retry attempts per process

# =============================================================================
# EXTERNAL SERVICES (ejemplos - descomentar según necesidad)
# =============================================================================
# API_EXTERNA_URL=https://api.example.com
# API_EXTERNA_TOKEN=your_api_token
# SFTP_HOST=sftp.example.com
# SFTP_PORT=22
# SFTP_USER=user
# SFTP_PASSWORD=password

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
# 1. NO crear archivos .env en los templates individuales (api, web, consola)
# 2. Todos los templates importan desde: repositorio_lib.config.settings
# 3. Para acceder a config:
#    from repositorio_lib.config.settings import db_settings, jwt_settings, app_settings
# 4. En producción, asegurar permisos restrictivos: chmod 600 .env
# 5. NUNCA commitear este archivo con valores reales en Git
# =============================================================================