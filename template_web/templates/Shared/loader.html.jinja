<!-- ========================================
     GLOBAL LOADER COMPONENT
     ========================================

     This is a reusable loader overlay component that can be
     controlled via the Loader JavaScript utility.

     It's included in layout.html.jinja and available globally.
-->

<div id="global-loader" class="loader-overlay">
    <div class="loader-container">
        <!-- Spinner Loader (default) -->
        <div class="loader-spinner"></div>

        <!-- Loader Text -->
        <div class="loader-text">Cargando...</div>
    </div>
</div>

<!-- Alternative Loader Styles (uncomment to use) -->

<!-- Dots Loader
<div id="global-loader" class="loader-overlay">
    <div class="loader-container">
        <div class="loader-dots">
            <span></span>
            <span></span>
            <span></span>
        </div>
        <div class="loader-text">Cargando...</div>
    </div>
</div>
-->

<!-- Pulse Loader
<div id="global-loader" class="loader-overlay">
    <div class="loader-container">
        <div class="loader-pulse"></div>
        <div class="loader-text">Cargando...</div>
    </div>
</div>
-->